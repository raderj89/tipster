%tr{id: "js-invitation-#{invitation.id}"}
  %td
    = invitation.recipient_email
  %td.right-column
    = link_to(image_tag('x.png', class: 'right js-delete-employee'), [current_employee, @property, invitation], method: :delete, data: { confirm: "Are you sure you want to delete this invitation?" }, remote: true)